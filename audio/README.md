# Background music

Put your MP3 in this folder and name it `background.mp3`. The home page and portfolio page both load that file automatically.

Browsers block websites from starting audible music before a user interacts with the page, so click **Start music** in the top-right corner. The button and page text will gently scale with the song's bass level.
